# REST API для управления пользователями на Express.js

Простое REST API, построенное на **Node.js** и **Express**, с хранением данных в локальном JSON-файле. Поддерживает все базовые операции CRUD (Create, Read, Update, Delete) и включает валидацию, защиту от дубликатов email и автогенерацию ID.

---

## 🚀 Функционал

- `GET /users` — получить всех пользователей
- `GET /users/:id` — получить пользователя по ID
- `POST /users` — создать нового пользователя
- `PUT /users/:id` — обновить данные пользователя
- `DELETE /users/:id` — удалить пользователя

### ✅ Особенности:
- Данные сохраняются в файле `users.json`
- Валидация полей: имя и email обязательны
- Проверка формата email
- Защита от дублирования email
- Автогенерация уникального ID
- Очистка пробелов в имени и email (trim)
- Чувствительность к регистру email устранена (всё приводится к нижнему регистру)

---

## 🛠️ Технологии

- Node.js
- Express.js
- JSON (локальное хранение)
- `fs` модуль (работа с файловой системой)

---

## 🔧 Как запустить

1. Убедись, что установлен [Node.js](https://nodejs.org/)
2. Склонируй репозиторий

Установи зависимости:
Bash
npm install

Запусти сервер:
Bash
npm start

Сервер будет доступен по адресу: http://localhost:3000
🧪 Тестирование API
Все запросы можно выполнять через curl, Postman, Thunder Client или любой HTTP-клиент.

1. Получить всех пользователей
Bash
curl http://localhost:3000/users
2. Получить пользователя по ID
Bash
curl http://localhost:3000/users/1

❌ Если ID не существует — вернёт 404 Not Found


3. Создать нового пользователя
Bash
curl -X POST http://localhost:3000/users \
  -H "Content-Type: application/json" \
  -d '{"name": "Bob", "email": "bob@example.com"}'

✅ Успешно: статус 201 Created

❌ Ошибки:


400 Bad Request — пустые поля или неверный email
409 Conflict — email уже занят

4. Обновить пользователя
Bash
curl -X PUT http://localhost:3000/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name": "Alice Smith", "email": "alice.smith@example.com"}'

✅ Успешно: возвращает обновлённый объект

❌ Ошибки:


404 — пользователь не найден
409 — email уже используется другим

5. Удалить пользователя
Bash
curl -X DELETE http://localhost:3000/users/1

✅ Ответ: сообщение об успешном удалении

❌ 404 — если пользователь не существует

Создано с ❤️ с использованием Express и Node.js.
